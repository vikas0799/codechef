#include<bits/stdc++.h>
#include<utility>
using namespace std;
bool cmp(int a,int b)
{
    return a>b;
}
int main()
{  
	int n;cin>>n;
    for(int i=0;i<n;i++)
    {
        pair<int,int>pr[i];
        pr[i].first=89;
        pr[i]=90;
    //cout<<pr;
    // sort(m[i], m[i] + N);//sort acording to row
   // sort(a.a+n,cpm(a));//sort w.r.t F[n]
    
    sort(a,a+4,cmp);

    for(int i=0;i<4;i++)
     // cout<<a[i]<<" ";

}